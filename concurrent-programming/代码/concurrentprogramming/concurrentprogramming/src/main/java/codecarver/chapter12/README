线程池原理与自定义线程
执行线程的方法只需要1毫秒，但是创建和结束线程要3毫秒。你肯定不干

1.任务队列
2.拒绝策略
    可以抛出异常（让外部人知道调用有问题，或者超过处理的能力），
    直接丢弃，
    阻塞，
    放到临时队列（任务队列已满）

3.初始化线程池（比如两个线程）
4.active(活跃的时候一般6个，所以两个不够，可以创建一个6个左右的线程池)
5.max 线程池中线程的最大个数

----
自定义线程池

