给应用程序注入钩子程序
// 应用场景 当程序出现问题的时候，主动告诉调用者，而不是由我们去监测


如何捕获线程运行期间的异常
线程里面是抛不出异常来的？线程的逻辑执行单元放在run方法里，run方法签名
不允许抛出异常，所以只能捕获，万一线程里出现runtimeException，线程会挂掉，
但是应用程序不会挂掉，所以我们只能从log里或者控制台看到。
在run方法里抛不出运行时异常

getStackTrace()
